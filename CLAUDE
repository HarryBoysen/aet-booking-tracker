# Booking Tracker — Working Rules

Short, practical rules for working on this project with Claude / Claude Code / Cowork.
No PR pipeline, no multi-role framework — just enough structure to avoid repeating mistakes.

## Before starting any task
- Read `STATUS.md` (and this file) first. Don't guess where the project stands.
- Confirm the ONE specific task to do now. Don't "continue where we left off" — state it explicitly.

## While working
- Make the smallest change that solves the task. No unrelated refactoring "while I'm in there."
- If a rule or convention already exists (see "Known rules" below), follow it — don't re-derive it.
- If you hit the same bug a second or third time without fixing it, STOP. Say so plainly, explain the likely root cause, and don't just try another small patch blind.

## After a change
- Summarize: what changed, what was deliberately NOT touched, what should be manually tested (there is no automated test suite — Harry tests by hand).
- Update `STATUS.md`: what works now, what's still broken/open.
- Record any new decision or rule directly in this file, immediately — not just in chat.

## Known rules (keep this list updated)
- `seedDefaultsIfEmpty()` must always stay disabled.
- All form labels must be in English.
- Correct partner names: Albatros Travel, Asien Travel, DANexplore, Unique Travel.
- Auth (2026-09-18 decision): real Firebase Auth (email/password) + Firestore Security Rules with an explicit employee-email allowlist, NOT the earlier abandoned Google Sign-In popup/redirect approach (that was unreliable on GitHub Pages). No public self-registration; Harry creates every account manually in Firebase Console. Built and tested (see STATUS.md), not deployed yet — do not assume it's live without checking STATUS.md.
- Access control (2026-09-18, superseded same day): first built with an admin-only-delete split (only Harry could delete). Harry then explicitly asked for this to be simplified: there is NO separate admin role — every allowlisted account has the same rights (read/create/update/delete). Harry's control point is the allowlist itself (who has an account at all), not a permission tier inside the app. Don't reintroduce an admin/delete distinction without Harry asking again.
- Presence ("who's online"): a `presence` Firestore collection, one doc per user keyed by their Firebase Auth uid, updated via a 45s heartbeat. A user counts as online if their heartbeat is under 2 minutes old — no cleanup job needed, stale docs just age out of the count.
- My Action List (2026-09-18, built, not yet deployed — see STATUS.md): new default landing tab, personal task queue. Reuses existing status/sent/reply/deposit/creditcard fields, no new data model for follow-up/payment. New additive fields: `h.assignedAgent` (escalation target, overrides `booking.agent` for that one hotel line) and `h.escalations[]` (trail). New `users` Firestore collection (email -> displayName/agentName/role) manages the login-email-to-agent-name mapping; `role: 'admin'` ONLY controls the "show everyone's tasks" toggle on My Action List — it is NOT the old delete-permission admin tier (that stays removed, see the access-control decision above) and does not affect delete rights. Fallback admin (before `users` is populated) is hardcoded to brian.harry.boysen@gmail.com only. `USER_DISPLAY_NAMES` was corrected at the same time to the real employee names Harry supplied (Harry, Kai, New, Natty, Sherly, Benz, Waan, Mi, Hem, Nipon, Bee, Air, Jonas, Winnie, Neung) — it previously had generic placeholder names. No firestore.rules changes needed (generic wildcard rule already covers any new collection).
- Bookings tab is no longer the default active tab (My Action List is). Any new Playwright test that opens the "New booking" modal must first click `[data-tab="bookings"]`.
- Rule-based follow-up + data validation (2026-09-18, built, not yet deployed — see STATUS.md): follow-up severity (neutral/amber/red) is computed only from `h.sent`/`h.reply`/`h.status`, never a manual status word, using `daysSince()`/`daysBetween()` which are now Thailand-time-based (`thailandTodayStr()`, Asia/Bangkok, no DST) instead of browser-local time — don't reintroduce raw `new Date()` day-diff math for anything follow-up-related. Thresholds live in Firestore `settings/followup` ({amberThresholdDays, redThresholdDays}, default 2/4), editable in the "(team)" modal — never hardcode new threshold values elsewhere. Booking form now requires guest name and Partner (Partner used to say "Optional" — it no longer is); validation errors are shown inline next to the field via `markFieldError()`/`clearFieldErrors()`, not `alert()`. Check-out must be strictly after check-in (same-day is now invalid, not just earlier) — the smart-default now suggests check-in+1 day, not same-day. Partner stays a `<select>` bound to the Partner master data on purpose; Agent still uses a datalist autocomplete (`agentSuggestions`) plus case-insensitive normalization on save (`normalizeAgainstList()`) so "sherly"/"Sherly" don't fork into two records — Hotel/supplier name and Room Type were upgraded past autocomplete to real dropdowns, see the next bullet. Duplicate-booking warning (same ref, or same guest + shared check-in date) always shows via `#duplicateModal`; only `role: 'admin'` (the same narrow My Action List role, not the old delete-admin tier) gets a "Save anyway" override — everyone else is hard-blocked. `booking.history[]` is an append-only change log (who/when/what) shown under the hotels table in the expanded Bookings-tab card.
- Hotel/Supplier + Room Type as real dropdowns (2026-09-18, built, not yet deployed — see STATUS.md): Hotel/Supplier Name is now a `<select class="hf-hotel-select">` (like Partner) sourced from `knownHotelNames()`, with a "+ Add new hotel..." option (`value="__new"`) that reveals `<input class="hf-name-new">`. A hotel picked via "+ Add new hotel..." is auto-persisted as a new independent `hotelsCrm` doc on save (same schema as `saveCrmEntity()`) — this is the actual mechanism that prevents one hotel forking into two spellings, since it becomes a real selectable option for every future booking. Room Type is the same pattern, per hotel-line: `<select class="hf-rt-select">` + `<input class="hf-rt-name-new">`, sourced from `knownRoomTypeNamesForHotel(hotelName)` (that hotel's own Allotment + past bookings, case-insensitive; falls back to the full cross-hotel list when the hotel has no room-type history yet), rebuilt via `refreshRoomTypeLineOptions(block)` whenever the hotel selection changes. Deliberate asymmetry: unlike hotels, a new room type picked via "+ Add new room type..." is NEVER auto-written into an allotment's `roomTypes` array — room types have no independent master record, and inserting a bare `{name, qty:0}`-shaped entry into an existing allotment doc risks corrupting the Full/Low badge and sweep-line peak-occupancy math. Room types only get the same `normalizeAgainstList()` safety net on save. Both `saveBooking()`'s hotel-block loop and its `roomTypeFilled`/`hasAnyData` checks read from `.hf-hotel-select`/`.hf-name-new` and `.hf-rt-select`/`.hf-rt-name-new` — never reintroduce a plain free-text `.hf-name` or `.hf-rt-name` input, and any Playwright test driving these fields must use `page.selectOption()` (`.fill()` throws on a `<select>`), picking `'__new'` first when typing a brand-new name.
- `syncHotelsFromBookings()` (2026-09-18, built, not yet deployed — see STATUS.md): a button in the Hotels CRM view ("Add all hotels from bookings", visible only when `crmType==='hotels'`) that backfills a `hotelsCrm` doc for every name in `knownHotelNames()` not already present. Purely additive (never overwrites/deletes), so — unlike the rename/delete actions below — it's open to everyone, not admin-gated. Safe to run repeatedly (case-insensitive existence check, no duplicates).
- Admin-only rename/merge/delete for hotels and room types (2026-09-18, built, not yet deployed — see STATUS.md): a SCOPED exception to the "no admin tier" access-control decision above — it applies ONLY to renaming/deleting an EXISTING hotel (`hotelsCrm` record or an allotment's `hotelName`) or an EXISTING room type inside an allotment, because those actions rewrite historical booking data; it does NOT reopen the general admin/delete question, and does NOT restrict adding a brand-new hotel or room type (still open to everyone). Gated by the same narrow `isAdminViewer()` used elsewhere (My Action List's admin toggle, duplicate-warning override) — not a new role. Shared cascade helpers: `countHotelUsage(name)` / `countRoomTypeUsage(hotelName, roomTypeName)` (usage counts shown before any admin confirm), `renameHotelEverywhere(oldName, newName, opts)` and `renameRoomTypeEverywhere(hotelName, oldName, newName)` (rewrite every matching booking's hotel-line/room-type text, plus allotments/hotelsCrm for the hotel case — never silent, always logs a `booking.history[]` entry). Rename-detection compares the OLD vs NEW name as an exact trimmed string (not case-insensitive) — a pure case fix (e.g. "rawi warin resort" → "Rawi Warin Resort") must still trigger the cascade/merge, since that's precisely the kind of duplicate this exists to fix; `renameHotelEverywhere`'s own internal matching stays case-insensitive so it still finds the old spelling wherever it appears. If renaming a `hotelsCrm` record collides with another existing record of the new name, they're merged (notes combined) and the now-duplicate record deleted, rather than left as two cards with the same name. UI: `#crm-name` (CRM Hotels modal) and `#allot-hotel`/`.rt-name` (Allotment modal) are `disabled` for non-admin when editing something that already has a saved name (`openCrmModal`/`openAllotmentModal`/`addRoomTypeBlock` check `isAdminViewer()`); adding a brand-new hotel/room type (blank field, no prior name) stays unlocked for everyone. `removeRoomTypeBlock(btn)` replaced the old bare `.remove()` on the room-type "×" button — a never-saved blank line still removes freely, but removing an existing one requires admin + shows a usage-count confirm.
- Lomprayah Tickets / Train Tickets tabs (built 2026-09-23, REVISED 2026-09-23 per Harry's 6-point feedback on the first preview, still not yet deployed — see STATUS.md): replaces two team Excel sheets used to remember to book ferry/train legs for guests. Both tabs still read/write ONE Firestore collection, `transportTickets`, distinguished by `type: 'lomprayah'|'train'`, sharing `renderTransportTab(type)`, `openTicketModal(type, ticket)`, `saveTicket()`, `deleteTicket(id)`, `toggleTicketFlag(id, field)`.
  - **Booking link**: `bookingId` (optional FK into `bookings`) lets several tickets share one booking (several ferry/train legs per booking). `#tk-booking` was REVISED 2026-09-23 from a `<select>` to a searchable `<input list="ticketBookingOptions">` — Harry reported a plain dropdown is unusable once there are several hundred bookings and asked to be able to search by number. `refreshTicketBookingSelect()` (name kept for minimal diff) now builds the `ticketBookingOptions` datalist via `ensureDatalist()` AND a module-level `ticketBookingLabelMap = {}` mapping the exact `"ref — guest"` (or `"ref — guest (Archived)"`) label string (via `ticketBookingLabel(b)`) to each booking's id, rebuilt on every modal open. It also now deliberately includes ARCHIVED bookings (previously filtered out with `bookings.filter(b => !b.archived)`) — Harry's explicit ask, since a transport ticket can still be outstanding after the hotel booking itself is archived. `window.onTicketBookingChange()` resolves the current input text against `ticketBookingLabelMap` (exact match only) into `ticketLinkedBookingId`, then auto-fills + disables `#tk-guest`/`#tk-partner` and hides the manual `#tk-bookingref` field when resolved, or reverts both when the text doesn't match anything known (including an empty/cleared field). `saveTicket()` re-resolves from the live input text rather than trusting stored state (defends against e.g. browser autofill bypassing the input event). `openTicketModal()` looks the ticket's `bookingId` up across ALL of `bookings` (not just active) and pre-fills the search box with that booking's label text. Responsible Agent (`agent`) is still pre-filled from the linked booking but stays independently editable (only fills if empty, never overwrites).
  - **Follow-up**: `bookingOpensDate` (informational) and `followUpDate` (drives My Action List) are separate manually-set date fields — deliberately NOT a fixed rule, per Harry's explicit ask. `actionListRows()` now also returns `out.transport`: any ticket with `!cancelled && !booked && followUpDate && followUpDate <= today`, filtered by `agent` matching the viewer unless `viewingAll` (same pattern as the hotel-based sections). Severity red if departure ≤3 days out, else amber. Rendered via `ticketActionRowHtml(r, viewingAll)`, passed into `actionSectionHtml(title, list, viewingAll, rowFn)` — a new optional 4th param, defaulting to the original `actionRowHtml`, added specifically so this structurally different `{t}`-shaped row (vs. the hotel sections' `{b,h}`) can reuse the section wrapper. `window.markTicketBookedFromAction(id, e)` is the row's "Mark booked" quick action (delegates to `toggleTicketFlag(id,'booked')`), which removes the row from the action list because it now flips `booked=true`.
  - **Booking/payment/sending separated**: `booked`/`bookedDate`, `paid` (no auto-stamp — `paymentDueDate` is a separate always-editable field, not tied to `paid`), and `sentToPartner`/`sentBy`/`sentDate` are three fully independent fields, each toggleable inline from the list (`toggleTicketFlag`) or in the modal. `sentBy` records the agent name (`myAgentName() || currentUserName`), shown via `window.renderTicketSentByNote()` as either "Sent by X on date" (existing) or a "will be marked as sent by X today" preview (unsent).
  - **Ticket details**: `ticketNumbers` (free text, can hold several), `wagon`/`seat` (train-only, `#tk-wagon-seat-row` display-toggled by `isTrain`). Ticket-file attachment was built via Firebase Storage (upload-on-select into `attachmentName`/`attachmentUrl`) in the first pass of this revision, then REMOVED again same day at Harry's request (he decided AET doesn't need it) — no Storage import, no `getStorage`/`storage` init, no `#tk-attachment-file` field, no `attachmentName`/`attachmentUrl` on the ticket doc, and the `fbstub/firebase-storage.js` test stub + its `gen_test_page.py` substitution entry were deleted. This also means the Blaze-plan upgrade is no longer needed for this feature — only for the separate, still-paused email-notification feature below, if/when that's built. Don't reintroduce attachment upload without Harry asking again.
  - **Dates/pax**: `departureDate`/`departureTime`/`arrivalDate`/`arrivalTime` are four separate fields (`window.onTicketDepDateChange()` defaults `arrivalDate` to `departureDate` only if arrival is still empty, for the common same-day case — never overwrites a set overnight-train date). `formatTicketDate(dateStr)` renders "11 Jan 2027" everywhere a ticket date is shown, to remove day/month ambiguity. `adults` (number) + `childrenAges` (array of per-child ages, held in module-level `ticketChildrenAges`, rendered via `window.renderTicketChildren()`; each row's inline `oninput` calls `window.setTicketChildAge(idx, val)` rather than assigning the module-scoped array directly — inline HTML attribute handlers run in global/window scope, not module scope, so a bare `ticketChildrenAges[i]=...` reference in an attribute string throws `ReferenceError`) with a live-computed `totalPax` shown as "Total pax: N".
  - **List as work overview**: `transportFilterState = {lomprayah:'all', train:'all'}` plus `window.setTransportFilter(type, filter, el)` drive five filter buttons per tab — `all` (labelled "Upcoming", the default: `!departureDate || departureDate >= today`), `notbooked`, `notsent`, `next7` (`departureDate` within `[today, today+7]`), `history` (`departureDate < today`) — plus a responsible-agent `<select>` (`refreshTicketAgentFilterOptions(type)`, built from tickets of that type) and the existing search box / Show cancelled checkbox, all AND-ed together before the soonest-date-first sort.
  - Small fix: Train tab's From/To example placeholders now read "Chiang Mai"/"Bangkok" (set both statically in the HTML and dynamically in `openTicketModal` based on `isTrain`) instead of the old boat-stop example ("Koh Samui Pier 12:30").
  - NOT built this round (out of scope — flagged to Harry, not silently dropped): a dedicated bulk-import wizard from the old Excel sheets with an uncertain-case review screen (mixed dates/status/remarks, multi-row trips). Harry mentioned this as "an important check needed on import" but it wasn't in his stated top priority (booking link, Action List follow-up) — can be built as a separate, isolated feature if/when he wants the old Excel data actually migrated in bulk.
  - Tests: `test_transport_tickets.js` fully rewritten against the new schema (booking-link auto-fill+lock, adults/children total, arrival-date default, independent Booked/Paid/Sent stamps incl. sentBy, all 4 list filters + agent filter + Upcoming/History split, My Action List integration incl. Mark booked). Full 22-script regression suite passes with no regressions from the `actionListRows`/`actionSectionHtml`/`renderActionList` signature changes.
- Shared team note on My Action List (built 2026-09-23, not yet deployed — see STATUS.md): Harry asked for somewhere to write a note visible "på forsiden" (the front page — My Action List, since that's the default tab on login). Clarified with him: any logged-in team member can write/edit it (not admin-only), and it's ONE shared note, not a per-author list — saving replaces the whole thing. Stored as a single Firestore doc `settings/teamNote` (`{text, updatedBy, updatedDate}`), reusing the existing `settings` collection (same one `settings/followup` already lives in) — no new collection, no Firestore rules change needed, since the existing wildcard rule already covers it. State: module-level `teamNote = {text, updatedBy, updatedDate}` (populated in the `settings` onSnapshot listener alongside `followupSettings`) and `teamNoteEditing` (bool, whether the box is currently showing its textarea). `renderTeamNoteBox()` renders into `#teamNoteBox` (sits above the summary row inside `#panel-action`): empty + not editing → a lone "+ Add a note for the team" button (renders nothing bulky when unused); has text + not editing → the note in a highlighted box with an "author · date" line and an edit-pencil button; editing → a `<textarea id="teamNoteInput">` prefilled with the current text, plus Save/Cancel/Clear-note buttons (Clear only shown when there's existing text to clear). `window.editTeamNote()`/`window.cancelTeamNote()`/`window.saveTeamNote()`/`window.clearTeamNote()` are the handlers; `saveTeamNote()`/`clearTeamNote()` explicitly call `renderTeamNoteBox()` again after their own `await setDoc(...)` resolves and `teamNoteEditing` is set back to `false` — NOT redundant: `setDoc`'s own `settings` onSnapshot callback can fire synchronously inside `setDoc` itself (before the `await` in the caller even returns control), i.e. while `teamNoteEditing` is still `true`, so without this second explicit call the box gets stuck showing the edit form after a successful save. If touching this flow again, keep that ordering in mind — it's not a race condition that "usually" doesn't happen, it happens every time in the local fbstub harness (`notify()` is synchronous there) and is just as possible against real Firestore.
  - Tests: new `test_team_note.js` (empty state, write+display with author/date, edit pre-fills existing text, Cancel discards, Save overwrites rather than appends, Clear resets to empty). Full 23-script regression suite passes.
- Inline "Notes" column on the Lomprayah/Train Tickets list (built 2026-09-23, not yet deployed — see STATUS.md): Harry, after confirming the deployed feature works, asked to write a remark (e.g. "Can book to Nov") straight into each ticket's row rather than opening the edit modal. Added a `<th>Notes</th>` in `renderTransportTab(type)`'s table header (between Sent and the actions column, table `min-width` bumped 1050→1250 to fit) and a matching `<td>` with `<input type="text" class="ticket-note-input" value="${esc(t.notes||'')}" onchange="updateTicketNotes('${t.id}', this.value)">`. This is NOT a new field — it reads/writes the ticket's existing `notes` (the same one the modal's `#tk-notes` textarea edits), so editing inline and editing via the modal stay in sync automatically. New handler `window.updateTicketNotes(id, value)` mirrors `toggleTicketFlag`'s spread-and-setDoc pattern but for a text value, and short-circuits (skips the Firestore write) when the trimmed value hasn't actually changed from `t.notes`, saving a write on a plain focus-then-blur with no edit. Saves on the input's `change` event (blur or Enter), not on every keystroke, same UX as the rest of the app's text inputs. `.ticket-note-input` CSS added near `.hotels-table` rules.
  - Tests: `test_transport_tickets.js` extended (Notes column header present on both tabs, typing in a row's note input persists it and survives a tab-switch re-render). Full 23-script regression suite passes.
- Compact Lomprayah/Train Tickets list (built 2026-09-23, not yet deployed — see STATUS.md): follow-up to the Notes column above. Harry asked for three things after seeing that screenshot: rows on one line (smaller text if needed), remove "Paid" from the list to free up space, and a more visible line between rows. New CSS class `.ticket-table` (added alongside `.hotels-table`, does NOT modify `.hotels-table` itself since that class is shared with other tables like Response Stats/CSV import): `font-size:12px` (was inheriting 13px from `.hotels-table`), `td{white-space:nowrap; padding:6px 10px; border-bottom:1px solid #e6e6e1}` (stronger border than `.hotels-table`'s default `0.5px solid #f5f5f3`), `th{padding:6px 10px; white-space:nowrap}`. Applied via `class="hotels-table ticket-table"` on the table in `renderTransportTab(type)` (table `min-width` reduced 1250→1150 now that Paid's column is gone). Removed the `<th>Paid</th>` header cell and its `<td><input type="checkbox" ... 'paid' ...></td>` row cell entirely from the list — the underlying `paid`/`paymentDueDate` fields and the modal's `#tk-paid` checkbox are untouched, so payment status is still tracked and editable via the edit modal (pencil icon), just not as a quick-toggle in the list view anymore. `white-space:nowrap` on `.ticket-table td` is what forces one-line rows — long guest names/routes no longer wrap, they just extend the row (the table sits inside the existing `overflow-x:auto` wrapper, so very wide content scrolls horizontally rather than breaking rows).
  - Tests: `test_transport_tickets.js`'s inline-toggle test updated — the Booked/Sent checkbox locator was `checkboxes.nth(0)`/`nth(2)` (skipping index 1 = Paid); now `nth(0)`/`nth(1)` since Paid's checkbox no longer exists in the row. Full 23-script regression suite passes.
- Removed the forced horizontal scrollbar on the ticket table (built 2026-09-23, not yet deployed — see STATUS.md): Harry sent a screenshot asking about a "pushing" gray line at the bottom of the table — that was the horizontal scrollbar on the `overflow-x:auto` wrapper div, which was ALWAYS visible because the table had a hardcoded `style="min-width:1150px"` while the page container (`.page{max-width:1200px}` minus `padding:1.5rem` on both sides) only has ≈1152px of usable width — a hair's-width overflow that forced the scrollbar on even when the actual column content didn't need it (worst on the Lomprayah tab, which has one fewer column than Train yet was still forced to the same 1150px). Fix: dropped the inline `min-width`, gave the table its own `width:auto` (via the new `.ticket-table` class, which cascades after `.hotels-table{width:100%}` in the stylesheet and so wins), and renamed the wrapper `<div style="overflow-x:auto">` to `<div class="ticket-table-scroll">` with dedicated CSS (`scrollbar-width:thin` for Firefox, `::-webkit-scrollbar{height:6px}` for Chrome/Edge) so IF a narrow window ever does need to scroll, it's a slim unobtrusive bar instead of the browser's chunky default. Also shrank `.ticket-note-input` from 170px→150px to help the row fit. `.ticket-note-input` and the `.ticket-table*` rules are the only rules touched — `.hotels-table` itself (shared with Response Stats etc.) is untouched.
  - Tests: new `test_ticket_multiroute_and_scroll.js` asserts `scrollWidth <= clientWidth` on both the Lomprayah and Train `.ticket-table-scroll` wrappers at a 1440px window (i.e. no forced scroll at a normal laptop width). Full 24-script regression suite passes.
- Multi-route Lomprayah ticket entry (built 2026-09-23, not yet deployed — see STATUS.md): Harry pointed out Lomprayah bookings usually need 2-3 separate route legs per customer (e.g. island-to-island then on to the mainland) and asked for an easier way to add them all against one booking. Deliberately did NOT change the data model — each route is still its own `transportTickets` document, same as today, so per-route Booked/Sent/Notes tracking still works exactly as before. Instead, refactored `window.saveTicket()`'s body into a new `saveTicketCore()` (does validation + builds the ticket object + `setDoc`, returns `true`/`false`, does NOT touch the modal) and added `window.saveTicketAndAddRoute()` alongside the existing `window.saveTicket()` wrapper. `saveTicketAndAddRoute()` calls `saveTicketCore()`, and on success sets `editingTicketId = null` (so the *next* save creates a new document instead of overwriting this one) then clears only the route/timing fields (`tk-from`, `tk-to`, `tk-depdate`, `tk-deptime`, `tk-arrdate`, `tk-arrtime`, `tk-vehicleno`, `tk-class`), follow-up fields, and status/ticket-detail fields (`tk-booked`, `tk-paid`, `tk-paymentdue`, `tk-sent`, `tk-ticketnos`, `tk-invoiceno`, `tk-price`, `tk-notes`, `tk-cancelled`) back to their defaults — booking link, guest, partner, agent, partner ref, booking ref, and adults/children (`ticketChildrenAges`, untouched) all carry over since they're the same for every route on the booking. Shows a green confirmation (`#ticketRouteAddedMsg`, styled distinct from the existing red `#ticketFormError`) and refocuses `#tk-from`. New button `#tk-addroute-btn` in the modal footer ("Save & add another route"), shown/hidden in `openTicketModal()` via `isTrain` — hidden for Train (`display:none`) since multi-leg train tickets aren't the norm Harry described; both `openTicketModal()` and `saveTicketAndAddRoute()` hide/show `#ticketRouteAddedMsg` appropriately so it doesn't linger into an unrelated ticket.
  - Tests: `test_ticket_multiroute_and_scroll.js` also covers this — opens a new Lomprayah ticket, links it to a booking, saves a first route via "Save & add another route", asserts the modal stayed open with booking/guest carried over and route fields cleared, enters a second route, finishes with the normal "Save Ticket", then asserts 3 total tickets exist for that booking (the original + 2 new routes), all sharing the same guest/bookingRef. Also asserts `#tk-addroute-btn` is hidden for a new Train ticket. Full 24-script regression suite passes.
- Grouped Lomprayah routes into one card per customer, and dropped the Notes placeholder text (built 2026-09-23, not yet deployed — see STATUS.md): follow-up to the two items above — Harry asked (1) for the Notes box to be a plain white input with no "e.g. Can book to Nov" placeholder, and (2) for a customer's multiple Lomprayah routes to visually sit together "the same box as when a customer has multiple hotels" — i.e. mirroring the `.booking-card`/`.card-header`/`.card-body` pattern `renderBookings()` already uses for a booking's hotel lines (see line ~2289-2317). (1) is a one-line removal of the `placeholder="e.g. Can book to Nov"` attribute on the `.ticket-note-input` in the row template. (2) is a bigger restructure of `renderTransportTab(type)`: it now branches on `isTrain` — Train keeps the exact old flat `<table>` (extracted into a new `ticketFlatRowHtml(t, todayStr)` helper, unchanged output) since multi-leg train journeys aren't the norm; Lomprayah instead groups `list` (the already filtered+sorted array) into `groups` keyed by `t.bookingId` when set, else by the lowercased trimmed guest name as a fallback for unlinked tickets — `forEach` preserves the list's existing soonest-first order, so groups naturally land in soonest-first order by their earliest route too, with no extra sort needed. Each group renders as one `<div class="booking-card">` reusing the SAME CSS classes as `renderBookings()`'s cards (`card-header`, `card-title`, `card-ref`, `card-guest`, `card-agent` — no new CSS needed for the card shell itself), with the header showing the group's shared bookingRef/guest/pax/agent/partner once (taken from `routes[0]`, i.e. the first/soonest ticket in the group) plus a "`N routes`" badge when `routes.length > 1`, and the card body holding a `<table class="hotels-table ticket-table">` with a NARROWER column set than before — just Departure/Route/Booked/Sent/Notes/actions, via the new `ticketRouteRowHtml(t, todayStr)` helper — since Guest/Pax/Booking ref/Partner/Agent moved to the header and would otherwise repeat on every route row. `card-header` gets `style="cursor:default"` since these cards don't collapse/expand (no click handler attached, unlike the Bookings tab's cards). Extracted the shared overdue/follow-up-due/style/date-label logic (previously duplicated per callsite) into `ticketRowMeta(t, todayStr)`, used by both `ticketRouteRowHtml` and `ticketFlatRowHtml`.
  - Tests: `test_ticket_multiroute_and_scroll.js` extended — after the 3-route "Save & add another route" flow, asserts exactly ONE `.booking-card` under `#lomprayahList` contains "Mads Storm Andersen" (not 3 separate cards/rows), that its `.card-body table` has exactly 3 route `<tr>`s (row count minus the header row), and that its `.card-title` text includes "3 routes". Also asserts `.ticket-note-input`'s `placeholder` attribute is falsy. The horizontal-scroll assertion from the same test file was updated to iterate ALL `.ticket-table-scroll` elements under `#lomprayahList` (one per card now, previously one table for the whole tab) rather than just the first. Also updated `test_transport_tickets.js`'s inline Booked/Sent toggle test — the old `#lomprayahList table tr:has-text("Mads Storm Andersen")` locator no longer matches anything (guest name now lives in the card header `<div>`, not a `<tr>`), replaced with `#lomprayahList .booking-card:has-text("Mads Storm Andersen")` scoped to find the checkboxes inside that card. Full 25-script regression suite passes.
- Visible save-error handling (2026-09-19, built, not yet deployed — see STATUS.md): `window.saveBooking()` had NO error handling — if the `await setDoc(...)` call (or anything else in the function) threw, the promise rejected silently: modal stayed open, no toast, no banner, nothing. This is exactly what Harry hit (changed a check-out date, "Save Booking" did nothing, no error shown) and was a real gap regardless of what his specific root cause turns out to be. Fixed by wrapping the whole function body in try/catch: any failure now shows a red banner in `#modalFormError` ("Could not save: <message>...") and logs the full error to the console via `console.error`. Root cause of Harry's specific report is still unknown (no live access to his data/session) — this only guarantees FUTURE failures are visible instead of silent. Don't remove this try/catch when touching `saveBooking()` again.
- "Possible duplicate hotel names" panel in the Hotels CRM tab (2026-09-19, built, not yet deployed — see STATUS.md): answers Harry's "check the hotels and send me a list of look-alikes" — since this environment has no live access to the real Firestore data, the fix is a permanent self-service panel (`#crmHotelDuplicatesWrap`, only rendered when `crmType==='hotels'`) rather than a one-off list. Reuses the existing `findPossibleDuplicateHotelNames`/`duplicateGroupKey`/`dismissedDuplicates` infrastructure already shipped for the Response Stats tab's `#statsDuplicatesWrap`, but scoped to the FULL `knownHotelNames()` list (CRM + allotments + bookings) instead of just `Object.keys(map)` (booking-lines only) — this is what catches a CRM-only look-alike pair with zero bookings, exactly Harry's own "Koh Kood Paradise Beach Resort" / "Koh Kood Paradise Resort" screenshot example. Detector improvement (shared by BOTH panels): `findPossibleDuplicateHotelNames` previously only matched prefix/suffix-style substring variants; it now ALSO matches when the shorter normalized name's words (≥3 words) are all present in the other name, regardless of order/position — needed because Harry's own example has a word ("Beach") inserted in the MIDDLE, which is not a contiguous substring match. The 3-word-minimum guards against false positives between unrelated hotels that merely share 1-2 generic words (e.g. two different "... Resort"s). New merge path (admin-only, same `isAdminViewer()` gate as the rename/delete work above) is WIDER than the existing `mergeDuplicateHotelNames` (bookings-only): `mergeHotelNameGroup(idx)` calls `renameHotelEverywhere()` per non-canonical name (covers bookings + allotments + hotelsCrm), then `mergeHotelsCrmRecordsByName(canonicalName)` collapses any resulting same-named `hotelsCrm` docs into one (combined notes, earliest doc kept) — needed because `renameHotelEverywhere` renames one CRM record at a time and can leave two CRM cards sharing the same new name. `dismissHotelNameGroup(idx)` writes to the same `dismissedDuplicates` collection/key as the Stats-tab version, so a dismissal in either tab hides the group in both. Non-admins still see the panel and can dismiss, just not merge. Tested in `test_crm_duplicate_finder.js`.

## Model choice
- Default to the standard model (Sonnet) for normal fixes and features.
- Only escalate to a heavier model for genuinely hard structural decisions (e.g. finally solving the login/auth problem properly).

## STATUS.md template
```
Status: <one line — e.g. "Live, in daily use by 10-person team">
Known issues: <list, most important first — e.g. "Auth unresolved, running unsecured">
Last change: <date + one line>
Open questions: <anything waiting on Harry's decision>
```
